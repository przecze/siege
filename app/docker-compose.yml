version: '3.9'
services:
  game:
    build: .
    ports:
      - '127.0.0.1:8000:8000'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      VIRTUAL_HOST: siege.janczechowski.com
      VIRTUAL_PORT: 8080
      LETSENCRYPT_HOST: siege.janczechowski.com
    networks:
      - nginx-proxy
      - localnet

  test:
    build:
      context: .
      dockerfile: Dockerfile.test
    depends_on:
      - game
    environment:
      - BASE_URL=http://game:8000
    networks:
      - localnet
    profiles:
      - test

networks:
  localnet:
  nginx-proxy:
    external: true
