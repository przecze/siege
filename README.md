# Siege!!

Craft your units with pattern matching to overcome the opponent castle.

Two castles sit at opposite ends of a 1D battlefield. Players spawn units from their castle that advance toward the enemy, fighting any opponents they encounter. Crafting units happens on a 2D grid with blocks representing four resources — move the blocks to form a pattern and the corresponding unit spawns.

## Required Assets

This project uses third-party sprite assets that **cannot be redistributed** and are therefore not included in the repository.

### Tiny RPG Character Asset Pack v1.03 by Zerie

- **Download:** <https://zerie.itch.io/tiny-rpg-character-asset-pack> ($2.50 for the full 20-character pack)
- **License:** Personal and commercial use allowed; modification allowed; **redistribution prohibited.**

After downloading, place the zip (or extracted contents) in the `assets/` directory at the project root:

```
siege/
├── assets/                          ← .gitignored, create if missing
│   └── Tiny RPG Character Asset Pack v1.03b -Full 20 Characters.zip
├── app/
├── .gitignore
└── README.md
```

The `assets/` directory is listed in `.gitignore` and will not be committed.
